/**
 * Selects the <tt>path</tt> key.
 *
 * @function
 * @param {Object} state - redux store state
 * @return {Number} the state data of path
 * {@link module:users/constants::INITIAL_STATE constants::INITIAL_STATE}).
 */
export const getPath = (state: any) => state.path;
